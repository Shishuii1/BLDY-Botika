# Use /api with Vite proxy (recommended) — see vite.config.js
VITE_API_URL=/api
VITE_SOCKET_URL=http://localhost:5000

# Or point directly at backend (must match backend PORT in backend/.env)
# VITE_API_URL=http://localhost:5000/api
